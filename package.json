{
  "name": "dayflow",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "description": "Dayflow — deployed on Vercel: static PWA at the root + the API as a serverless function in /api (wraps server/src/app.js).",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "nodemailer": "^9.0.1",
    "pg": "^8.13.1",
    "web-push": "^3.6.7"
  }
}
